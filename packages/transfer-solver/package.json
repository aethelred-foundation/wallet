{
  "name": "@aethelred/wallet-transfer-solver",
  "version": "0.1.0",
  "private": true,
  "description": "Concrete intent-router Solver for TransferIntent — submits a direct on-chain ERC-20 or native-value transfer via a pluggable chain provider, then polls for receipt. Second production-shape solver after x402-facilitator, proving the solver pattern composes across intent kinds.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@aethelred/wallet-intent-router": "file:../intent-router",
    "@aethelred/wallet-notarization": "file:../notarization",
    "@aethelred/wallet-observability": "file:../observability"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
