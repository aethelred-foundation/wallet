package xyz.aethelred.wallet.di

/**
 * Placeholder for future bindings that need specially-qualified Android
 * contexts (e.g. `@ActivityContext`, `@FragmentScoped`) or platform
 * services (ClipboardManager, KeyguardManager).
 *
 * Today everything can be constructor-injected via Hilt's built-in
 * `@ApplicationContext` qualifier, so this module deliberately ships
 * empty. Leaving the file to give the Android team a home for the next
 * round of bindings without another DI refactor.
 */
