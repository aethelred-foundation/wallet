defmodule TerraquraVerificationWorkflowsTest do
  use ExUnit.Case
  doctest TerraquraVerificationWorkflows

  test "greets the world" do
    assert TerraquraVerificationWorkflows.hello() == :world
  end
end
